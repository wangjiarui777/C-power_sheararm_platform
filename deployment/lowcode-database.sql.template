-- Render placeholders through the deployment secret manager. Never commit a rendered copy.
CREATE DATABASE IF NOT EXISTS `${LOWCODE_SCHEMA}` CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER IF NOT EXISTS '${LOWCODE_DB_USERNAME}'@'${LOWCODE_DB_HOST}' IDENTIFIED BY '${LOWCODE_DB_PASSWORD}';
ALTER USER '${LOWCODE_DB_USERNAME}'@'${LOWCODE_DB_HOST}' IDENTIFIED BY '${LOWCODE_DB_PASSWORD}';
REVOKE ALL PRIVILEGES, GRANT OPTION FROM '${LOWCODE_DB_USERNAME}'@'${LOWCODE_DB_HOST}';
GRANT SELECT, INSERT, UPDATE, DELETE, CREATE, ALTER, INDEX ON `${LOWCODE_SCHEMA}`.*
  TO '${LOWCODE_DB_USERNAME}'@'${LOWCODE_DB_HOST}';
FLUSH PRIVILEGES;
