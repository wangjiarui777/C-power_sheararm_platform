<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.SysUserDeviceMapper">
    <select id="selectDeviceIdsByUserId" resultType="Long">
        select device_id
        from sys_user_device
        where user_id = #{userId}
        order by device_id
    </select>

    <delete id="deleteByUserId">
        delete from sys_user_device where user_id = #{userId}
    </delete>

    <delete id="deleteByUserIds">
        delete from sys_user_device where user_id in
        <foreach collection="userIds" item="userId" open="(" separator="," close=")">
            #{userId}
        </foreach>
    </delete>

    <insert id="batchInsert">
        insert into sys_user_device(user_id, device_id, create_by, create_time)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.userId}, #{item.deviceId}, #{item.createBy}, now())
        </foreach>
    </insert>
</mapper>
